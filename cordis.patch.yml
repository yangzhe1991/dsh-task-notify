# @yangzhe1991/dsh-task-notify 配置层:注册插件行。
# 作为 profile bundle 时,本层叠加在 dsh-base 之后;
# 用户可在自己 profile 的 cordis.patch.yml 里按 id 覆盖或禁用此行。
- insert:
    - id: task-notify
      name: '@yangzhe1991/dsh-task-notify'
